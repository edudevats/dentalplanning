# Core
Flask==3.1.*
Flask-SQLAlchemy==3.1.*
Flask-Migrate==4.0.*
Flask-JWT-Extended==4.7.*
Flask-CORS==5.0.*

# Validation
marshmallow==3.23.*
marshmallow-sqlalchemy==1.1.*

# Database (SQLite built-in, mysqlclient for MySQL/PythonAnywhere)
SQLAlchemy==2.0.*
mysqlclient>=2.2

# Excel (for import)
openpyxl==3.1.*

# HTTP client (Clip API)
requests==2.32.*

# Utilities
python-dotenv==1.0.*
tzdata==2026.2

# Testing
pytest==8.3.*
pytest-flask==1.3.*

# Procesamiento de imágenes (normalización del logo al subir)
Pillow>=10.4

# Facturación CFDI
satcfdi==4.9.*
qrcode==8.*
cryptography>=44
defusedxml>=0.7
# PDF de respaldo del CFDI (Finkok normalmente devuelve el PDF con accept=XML_PDF;
# weasyprint es el fallback). Requiere libs del sistema (pango/cairo) en el servidor.
weasyprint>=60
